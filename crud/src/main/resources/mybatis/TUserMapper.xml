<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.herion.dao.TUserMapper">
    <select id="selectAll1" resultType="com.herion.pojo.TUser">
        select * from t_user ${ew.customSqlSegment}
    </select>

    <select id="selectUserPage" resultType="com.herion.pojo.TUser">
        select * from t_user ${ew.customSqlSegment}
    </select>
</mapper>